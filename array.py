lst=[]
num=int(input("how many numbers:"))
for n in range(num):
  numbers=int(input("Enter number")
  lst.append(numbers)
  print("maximum element in the list is:",max(lst),"\nMinimum element in th list is:",min(lst))
